In this section we continue to learn how to work with node module system, in the last section we explored this functionality using the `fs` module. In this section we will start to put all of that together and actually create notetaking application

In this section the focus is on file system and the command line arguments. We will use the `process.argv` array to access the command line arguments passed to the Node.js application.

The file system is gonna allow us to store the user's data and command line argument will allow us to get the input from the user

Later on we will learn how to store data in a data base, and get input from a user using a web application